*.iml
.gradle/
.idea/
.DS_Store
.kotlin/

# Build outputs in any module (not just the root)
build/
/captures
.externalNativeBuild
.cxx

# Local machine config (SDK path)
local.properties

# Never commit signing material. The release key lives outside this repo
# (~/code/dewiuniapp-signing — kept its old name deliberately), in Actions secrets,
# and in the private backup repo —
# these patterns are the safety net against a stray local copy.
*.jks
*.keystore
keystore.properties
signing.properties

# Secrets and local env
.env
.env.*

# Logs and local agent scratch (worktrees, session files)
*.log
.claude/
__pycache__/
